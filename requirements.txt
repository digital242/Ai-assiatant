# Sumo Phase 1 dependencies. Pinned to reasonable minimum versions.

anthropic>=0.40.0        # Claude API SDK (Messages + tool use)
vosk>=0.3.45             # offline speech recognition (wake word + STT)
sounddevice>=0.4.6       # cross-platform mic capture (PortAudio)
pyttsx3>=2.90            # offline text-to-speech (default voice)
requests>=2.28.0         # Fish Audio TTS HTTP calls (when tts_backend: fish)
PyYAML>=6.0              # config.yaml parsing
python-dotenv>=1.0.0     # optional: load API keys from a .env file

# The HUD (run_hud.py) uses Tkinter, which ships with the standard CPython
# installer on Windows and macoS -- no package needed. On some Linux distros
# install it separately, e.g. Debian/Ubuntu: sudo apt install python3-tk

# --- Platform-specific TTS backends (installed automatically per OS) ---------
# macOS: pyttsx3 speaks through NSSpeechSynthesizer via pyobjc.
pyobjc>=9.0; sys_platform == "darwin"
# Windows: pyttsx3 speaks through SAPI5 via comtypes / pywin32.
comtypes>=1.1.14; sys_platform == "win32"
pywin32>=305; sys_platform == "win32"
